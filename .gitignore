# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr

# Bun cache (keep bun.lockb tracked for reproducible installs)
.bun

# TypeScript build info (you store in node_modules/.tmp but include anyway)
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Caches
.cache
.next
coverage
tmp
.temp

# Tailwind / framework caches (if created)
.postcss-cache
.stylelintcache

# Remove old unused ignore
# apiConfig.ts  (now using src/config/api.ts and should be committed)